import UnderContructure from '@/components/UnderConstructure';

export const metadata = {
    title: `${process.env.APP_NAME} - Login`,
    description: 'Halaman login untuk memulai session aplikasi',
};

const Login = () => <UnderContructure />;

export default Login;
